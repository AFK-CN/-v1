interface:
  display_name: "知识库"
  short_description: "总控：按索引、路由和输出契约调用本机知识库"
  default_prompt: "Use $knowledge-base to route this request through the local knowledge base without scanning protected data."
