# 知识库系统 3.0.0

发布日期：2026-07-19

工程版本：`3.0.0`

系统协议版本：`3.0`

## 发布范围

- P0：完成目录重组收口和可复现产物修复。
- P1：启用仅限正式知识层的混合检索。
- P2：启用通用表达资产契约和校验器；表达资产接入保持待确认。active account-learning v2.8 已启用通用 AI 输出来源隔离，不启动真实账号学习。

## 数据边界

- 不包含 `数据/`、`00_Inbox/`、`99_Archive/` 中的原始内容。
- 不包含 `20_User/` 私密数据、生产数据库、反馈或本机配置。
- 不包含 `00_System/runtime/` 缓存、日志或健康凭证。
- 轻量数据源的文本 manifest 可以进入本机 Git 基线，大型音视频和可复现帧图保留在本机或 NAS。

## 验收门

发布必须同时通过：

1. 全量单元测试；
2. 系统契约验证；
3. Runtime Doctor；
4. 用户层验证；
5. 系统边界与账号污染审计；
6. 分发审计；
7. 正式检索实库烟测、严格账号过滤和性能上限；
8. Git 生成产物一致性与干净基线检查。

## 回滚

- Git 标签：`v3.0.0`。
- 系统回滚规则：`00_System/shareable/skills/rollback.md`。
- P1 回滚不修改正式知识文件。
- 表达资产仍未接入 active account-learning；视觉来源隔离如需回滚，恢复 v2.7，候选、原始资料和正式账号内容不删除。
