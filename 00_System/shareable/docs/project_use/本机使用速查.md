# 本机使用速查

```text
@知识库 + 你的需求
我要处理资料 + 账号/资料范围
我要学习账号 + 账号/已处理证据范围
我要出选题 + 账号/数量/方向
我要写文案 + 账号/已确认选题/形式
我要复盘 + content_id/反馈来源
我要看状态
```

```bash
.venv/bin/python -m tools.kb.cli --root . skill-install
.venv/bin/python -m tools.kb.cli --root . init
.venv/bin/python -m tools.kb.cli --root . user-validate
.venv/bin/python -m tools.kb.cli --root . account-skills-sync
.venv/bin/python -m tools.kb.cli --root . validate-system
```

新电脑首次使用按 `skill-install -> init -> user-validate -> validate-system` 执行。

系统 Skill 在 `00_System/shareable/skills/active/`；账号 Skill 在各账号中心 `skill/`；注册表和生产记忆在 `20_User/`。默认不读取原始资料、完整生产数据库和用户私有目录。
