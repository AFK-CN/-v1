# 用户层

这是本机层，也是用户层。目录结构由系统定义并通过 `user-init` 建立，但真实数据不进入可分享系统包。

- `config/`：账号 Skill 注册表、粗学 profile 和默认行为。
- `data/`：选题、内容、反馈关联的本机 SQLite。
- `feedback/`：截图、表格和人工反馈原件。
- `private/`：用户私有内容。
- `local/`：NAS、机器路径和环境配置。
模型不得加载完整数据库；查重和聚合由代码完成，只返回小型摘要。
