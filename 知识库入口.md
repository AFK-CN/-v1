# 知识库入口

这是新对话和外部项目使用本机知识库的固定入口。默认入口是 `@知识库 + 你的需求`。

## 固定读取顺序

读取顺序由 `00_System/shareable/config/skill_contract.json` 维护：

1. `知识库入口.md`
2. `00_System/shareable/index/task_entry_index.md`
3. 按 `00_System/shareable/index/controller_routes.json` 命中任务后，只读取对应 Skill 和索引。

`10_Knowledge/evidence/index/knowledge_index.json` 是机器全量索引，不是模型默认阅读入口。禁止全盘扫库；默认不读取 `数据/`、`00_Inbox/`、`99_Archive/`、`20_User/private/`、`20_User/data/`、`20_User/feedback/`、`20_User/local/` 和 `00_System/runtime/`。

## 系统结构

- `00_System/`：可迁移的系统底座；核心只有内容处理、账号学习、内容复盘三个系统 Skill，以及路由、契约、代码和验证器。
- `10_Knowledge/`：正式知识、候选知识和证据索引。账号专属生产 Skill 位于各账号中心的 `skill/`。
- `20_User/`：本机/用户层，保存账号 Skill 注册、生产记忆、反馈、私有内容和本机配置。
- `90_Temp/`：一次性中间文件和导出。
- `99_Archive/`：已退役历史。
- `数据/`：受保护原始资料，只读。

分层权威源是 `00_System/shareable/config/layer_map.json`。

## 三条主链

```text
资料 → 内容处理 Skill → 标准证据与粗学计划
标准证据 → 账号学习 Skill → 用户审核 → 正式账号 Skill
正式账号 Skill + 本机生产记忆 → 选题/脚本/图文 → 反馈 → 内容复盘 Skill
```

### 内容处理

保留链接、SQLite、NAS、本地资料接入，以及下载、解析、转写、OCR、抽帧、去重、账号归属、广告隔离、扫描、粗学归类和深学计划。产物只进入候选层和 runtime，不写正式账号中心。

### 账号学习

按 `account_learning_pipeline.json` 执行七阶段学习。剧情、长文案、知识口播、图文和商业内容是同一流程下的不同观察适配器。视觉账号在阶段 6 必须组建按生产角色与风险覆盖的账号专属视觉参考候选包，并隔离账号原始正例、用户认可正例、用户拒绝反例和外部参考；反例只能用于验收。系统 Skill 只保存通用规则，不保存账号资产。阶段 6 必须生成不可调用的账号 Skill 候选包；用户审核后才写入账号中心。

### 账号 Skill 生产

选题和内容生产必须先从 `20_User/config/account_skill_registry.json` 解析账号 Skill。生成选题前，代码在本机 SQLite 中批量查重，只把最多几条冲突摘要返回给模型；选题确认和内容交付后写回 `topic_id`、`content_id` 与 Skill 版本。

### 内容复盘

复盘从一个 `content_id` 开始，绑定截图、表格、指标或人工反馈。它可以提出定向补学或账号 Skill 更新提案，但不自动修改任何 Skill。

## 初始化

新机器或 runtime 缺失时运行：

```bash
.venv/bin/python -m tools.kb.cli --root . skill-install
.venv/bin/python -m tools.kb.cli --root . init
```

`skill-install` 安装中英文全局入口，并用 `references/kb-root.json` 绑定当前知识库根目录。`init` 建立用户层、生产记忆数据库并同步账号 Skill 注册表。日常调用只运行健康门；单独重建用户层可用 `user-init`。

## 硬规则

- 原始资料不删除、不修改。
- 不新增正式子库，除非用户确认。
- 不从候选区直接调用知识。
- 不把某个账号的结构、表达或方向写进系统 Skill。
- 账号 Skill 更新必须有复盘证据、提案、用户确认和重新验证。
