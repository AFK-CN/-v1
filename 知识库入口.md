# 知识库入口

这是给 Codex 新对话读取的入口文件。

如果你在新对话里想使用本机知识库，只需要把这份文件加入上下文，或直接说：

```text
请读取 <KB_ROOT>/知识库入口.md，并按里面的规则使用知识库。
```

## 知识库位置

```text
<KB_ROOT>
```

## 你应该怎么使用这个知识库

默认不要全库扫描。固定入口读取顺序由 `00_System/shareable/config/skill_contract.json` 定义，并生成到 `00_System/shareable/skill_packages/知识库/` 和 `00_System/shareable/skill_packages/knowledge-base/`。

然后根据 `00_System/shareable/index/controller_routes.json` 和任务入口索引精准读取对应内容。`10_Knowledge/evidence/index/knowledge_index.json` 是全量机器索引，只给脚本、全量审计或用户明确要求时使用。除非用户明确要求，禁止扫描 `数据/`、`00_Inbox/`、`99_Archive/`、`80_Local/`、`20_User/private/`、runtime 和未确认的 Skill 提案。

## 当前分层结构

本知识库正在渐进迁移到分层结构，映射权威源是 `00_System/shareable/config/layer_map.json`：

- `00_System/`：系统层。`shareable/` 可分享，`runtime/` 不可分享。
- `10_Knowledge/`：知识层。`formal/` 是正式知识，`candidates/` 是候选知识，`evidence/` 是证据索引。
- `20_User/`：用户层。`syncable/` 可同步，`private/` 默认不分享、不外部调用。
- `80_Local/`：本机私有配置，默认阻断，不进入系统分享包。
- `90_Temp/`：临时层，只放短期输入、草稿、一次性中间文件和导出。
- `数据/`：受保护原始资料，默认不展开、不删除、不分享。

旧编号目录仍保持兼容，由索引器和 `layer_map.json` 映射到新层级。

## 总控调用原则

用户默认只需要说：

```text
@知识库 + 你的需求
```

也可以直接使用固定高级入口：

```text
我要出选题 / 我要写文案 / 我要复盘 / 我要入库 / 我要查账号 / 我要学习账号 / 我要看状态 + 具体需求
```

如果当前界面不识别中文入口，再使用兼容入口：

```text
knowledge-base + 你的需求
```

总控先用 `00_System/shareable/index/controller_routes.json` 判断任务类型，再按路由调度规划器、检索器、账号知识、内容生成、复盘、Skill 沉淀或系统审计能力。如果用户只给出高级入口但缺少最低信息，先反问最多 3 个关键问题，不直接生成。

## Initialization Lifecycle

新机器首次使用、`00_System/runtime/` 缺失、健康凭证缺失、runtime schema 不匹配或存在待执行旧目录迁移时，先运行：

```bash
.venv/bin/python -m tools.kb.cli --root . init
```

`kb init` 是独立且幂等的初始化生命周期，负责创建 runtime、写 manifest、迁移旧运行产物、重建可再生产物并生成同一 KB root 跨项目共享的当日健康凭证。

它禁止读取原始数据、修改正式知识、修改 active Skill、恢复任务或重跑任务。初始化后的日常调用只运行：

```bash
.venv/bin/python -m tools.kb.cli --root . health-gate
```

`health-gate` 不是 preflight，不遍历正式知识文件；当天凭证有效时直接进入用户路由。

如需创建优化后的目标目录骨架，运行：

```bash
.venv/bin/python -m tools.kb.cli --root . init-layers
```

## 索引优先原则

- 常规任务先查 `00_System/shareable/index/task_entry_index.md` 和 `10_Knowledge/evidence/index/knowledge_index_summary.md`，不要直接全盘找文件。
- `10_Knowledge/evidence/index/knowledge_index.json` 保留为全量机器索引，不作为 Codex 默认阅读入口。
- 代码批处理先看 runtime state 和 tasks，再决定是否需要运行脚本。
- 候选资产属于 `10_Knowledge/candidates/`，兼容旧候选目录和 runtime cache，但不等于正式知识。
- 正式知识目标沉淀到 `10_Knowledge/formal/`；旧正式知识目录继续兼容。
- 通用 Skill 属于系统层，生效位置是 `00_System/shareable/skills/active/`。

## 如果用户要处理 JSON

读取：

- `00_System/shareable/skills/active/JSON入库Skill_v1.md`
- `00_System/shareable/rules/JSON入库清洗规则.md`
- `00_System/shareable/rules/去重规则.md`
- `00_System/shareable/rules/case_cleaning/案例评分规则_v1.md`

执行原则：

- 原始 JSON 不修改。
- 先去重、清洗、评分。
- 再判断哪些内容进入知识库、哪些归档、是否需要新增子库。

## 如果用户要处理 SQLite 总库

默认总库路径：

```text
数据/sqlite_tables.db
```

执行原则：

- SQLite 总库仍属于受保护原始资料，默认不展开、不直接读取全文。
- 用户覆盖总库后，先运行 `tools.kb.cli sqlite-ingest --dry-run` 查看增量，再用 `--apply` 生成候选摘要、runtime 状态和轻量索引。
- `00_Inbox/sqlite_imports/` 只放新增/变更候选摘要，不做数据库完整镜像。
- `10_Knowledge/candidates/account_assets/sqlite_imports/` 放账号学习候选入口，用来承接现有账号学习流程。
- 评论表暂不处理；默认只处理作品/笔记内容，评论后续需要时再作为证据层单独接入。
- 后续仍进入账号学习流程：粗扫 -> 深度学习 -> 候选卡 -> 审核 -> 正式账号中心。

## 如果用户要处理截图

读取：

- `00_System/shareable/skills/active/截图复盘Skill_v1.md`
- `10_Knowledge/formal/reviews/feedback/反馈入库模板.md`

执行原则：

- 只读截图里看得清的指标。
- 看不清的不要猜。
- 判断截图对方法论、选题、周复盘是否有影响。

## 如果用户要处理表格

读取：

- `00_System/shareable/skills/active/表格复盘Skill_v1.md`
- `10_Knowledge/formal/reviews/weekly/周复盘模板.md`

执行原则：

- 先识别平台、字段、时间范围。
- 分平台复盘，不跨平台硬比。
- 输出表现好的内容、表现差的内容、方法论更新、下周建议。

## 如果用户要出选题或内容

先读 `00_System/shareable/index/controller_routes.json` 和 `00_System/shareable/index/task_entry_index.md`，再按任务读取：

- `10_Knowledge/formal/methods/`
- `10_Knowledge/formal/topics/`
- `10_Knowledge/formal/platforms/`
- `10_Knowledge/formal/content_factory/`

如果任务属于知识成长、自媒体、行动方法，再读取：

- `10_Knowledge/formal/accounts/知识成长自媒体方法论/`

如果任务提到账号、对标账号、姜胡说、出选题、写文案或口播，优先进入账号中心路由，不要只读通用方法论。

输出时要说明：

- 使用了哪个方法论。
- 适合小红书还是抖音。
- 为什么这样改。
- 本次读取了哪些知识层级，哪些是正式知识，哪些只是候选证据。

## 如果用户要周复盘

读取：

- `10_Knowledge/formal/reviews/weekly/周复盘模板.md`
- `10_Knowledge/formal/reviews/feedback/反馈入库模板.md`
- `20_User/syncable/preferences/用户偏好记录.md`
- `00_System/shareable/skills/active/`

输出：

- 有效方法论。
- 失效方法论。
- 下周选题。
- 需要补充的数据。
- 是否需要新的 Skill 提案。

## 硬规则

- 原始文件不删除、不修改。
- `数据/` 是受保护原始资料目录，默认不展开扫描。
- `80_Local/`、`20_User/private/`、runtime 默认不读取、不分享。
- 正式子库新增前必须先问用户。
- active Skill 修改前必须先写 proposal，用户确认后才能生效。
- 无效信息、重复信息、低价值资料不进入正式知识库。
