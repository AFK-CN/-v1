# 去重规则

## 三层去重

### 1. 精确 ID 去重

按平台主键判断：

- 小红书内容：`note_id`
- 抖音内容：`aweme_id`
- 抖音评论：`comment_id`
- 创作者：优先 `user_id`，必要时结合 `sec_uid`

同 ID 重复时，只选一个代表记录进入清洗结果。代表记录优先级：

1. 字段更完整。
2. 指标更新。
3. `last_modify_ts` 更新。

### 2. 文本近似去重

对 `title + desc/body + author_name` 生成文本指纹：

- 去空白。
- 去常见标点。
- 去话题符号、链接。
- 统一大小写。

文本指纹一致或高度相似时，标记为 `duplicate_near`。正式知识只引用代表记录，其余进入重复日志。

### 3. 主题变体识别

同一主题、同一人群、同一场景、同一结构，但表达或菜谱/案例不同，标记为 `topic_variant`。

主题变体不删除，因为它们能证明一个选题方向是否稳定有效。例如：

- 一周减脂餐合集。
- 独居一人食。
- 普通人每天 1 小时行动。
- 通过评论区理解用户需求。

## 当前数据去重观察

- 小红书内容：15 条，`note_id` 重复 0 条，文本指纹重复 0 条。
- 抖音内容：709 条，`aweme_id` 重复 111 条，文本指纹重复 136 条。
- 抖音评论：5970 条，`comment_id` 重复 0 条。
- 抖音创作者：2 条，唯一 `user_id` 1 个，重复 1 条。

