# 姜胡说正式学习与全量粗扫交接包

更新时间：2026-06-19
状态：全量粗扫已完成，正式深学待继续

## 1. 新会话先读

1. `知识库入口.md`
2. `14_KB_System/rules/本机使用速查.md`
3. `14_KB_System/tasks/pending/kb_task_20260617_video_learning_plan/direction_content_package_spec.md`
4. `14_KB_System/tasks/pending/kb_task_20260617_video_learning_plan/jianghushuo_primary_direction_download_plan.md`
5. `14_KB_System/tasks/pending/kb_task_20260617_video_learning_plan/queue_stdin_id_truncation_bug.md`
6. 本文件

禁止默认全盘扫库。`数据/` 和 `00_Inbox/` 只读；正式学习产物先进入候选学习区，不自动写正式知识库。

## 2. 已确认学习口径

- 一个视频只能归入一个主方向。
- 辅方向只做标签和检索，不重复学习。
- 一个视频完整学习一次，产出一张独立知识卡。
- 方向学习按一个方向连续学透，不在同一批次混多个方向。
- 每个方向形成三层产物：
  1. `方向方法论总结.md`
  2. `cards/` 下的一视频一文件知识卡
  3. `粗扫内容和选题.md`
- Top10 是本轮深学范围，不是长期知识卡数量上限。
- 结尾金句和互动引导只提取，不编造；没有就写 `未明确`。

## 3. 本轮正式学习范围

原计划129条，最终可学习127条。

以下两个视频只有文字元数据，没有视频和逐字稿，不再补下载，也不进入本轮深学：

| source_id | 原方向 | 处理决定 |
|---|---|---|
| `7163502772296486159` | 财富策略 | 该方向按9条学习 |
| `7264211001535483194` | 结构化理解 | 该方向按9条学习 |

最终13方向学习数量：

| 主方向 | 本轮深学数 |
|---|---:|
| 赚钱 | 10 |
| 财富策略 | 9 |
| 创业 | 10 |
| 商业机会 | 10 |
| 自媒体 | 10 |
| 短视频 | 10 |
| 表达文案 | 9 |
| 结构化理解 | 9 |
| 阅读输入 | 10 |
| 认知升级 | 10 |
| 个人成长 | 10 |
| 人生策略 | 10 |
| 技能沉淀 | 10 |
| **合计** | **127** |

目标清单来源：

`01_Case_Cleaning/video_learning/plans/jianghushuo_primary_top10.json`

新会话必须基于该清单排除上述两条，再生成127条正式学习清单。不得重新按多标签榜选片。

## 4. 已完成的深学内容

目前真正人工提炼完成12条：

- 赚钱：4条
- 创业：4条
- 阅读输入：4条

现有审核稿：

`01_Case_Cleaning/video_learning/learned_cards/20260618_A_jianghushuo_three_directions_existing.md`

注意：这12条目前仍在一个合集文件中，不符合最终“一视频一文件”。新会话先把它们拆成12张独立卡，再继续学习剩余115条，禁止重复分析同一视频。

## 5. 建议学习顺序

每次只处理一个方向。单次最多5条；同方向第二批紧接第一批，完成方向方法论草稿后再切换方向。

| 顺序 | 方向 | 总数 | 已深学 | 待深学 | 建议批次 |
|---:|---|---:|---:|---:|---|
| 1 | 赚钱 | 10 | 4 | 6 | 3+3 |
| 2 | 创业 | 10 | 4 | 6 | 3+3 |
| 3 | 阅读输入 | 10 | 4 | 6 | 3+3 |
| 4 | 技能沉淀 | 10 | 0 | 10 | 5+5 |
| 5 | 自媒体 | 10 | 0 | 10 | 5+5 |
| 6 | 短视频 | 10 | 0 | 10 | 5+5 |
| 7 | 商业机会 | 10 | 0 | 10 | 5+5 |
| 8 | 财富策略 | 9 | 0 | 9 | 5+4 |
| 9 | 结构化理解 | 9 | 0 | 9 | 5+4 |
| 10 | 认知升级 | 10 | 0 | 10 | 5+5 |
| 11 | 个人成长 | 10 | 0 | 10 | 5+5 |
| 12 | 人生策略 | 10 | 0 | 10 | 5+5 |
| 13 | 表达文案 | 9 | 0 | 9 | 5+4 |

每完成一个方向：

1. 核对该方向全部单卡字段完整。
2. 更新该方向 `方向方法论总结.md`。
3. 更新该方向 `粗扫内容和选题.md`。
4. 报告本方向完成数、缺失证据和未明确项。

## 6. 单卡验收

每张卡必须包含：

- `source_id`
- 原抖音视频链接
- 主方向、辅方向
- 为什么值得学习
- 核心观点
- 黄金3秒钩子
- 完整内容结构
- 结尾金句/互动引导；没有就写 `未明确`
- 视频层学习
- 可复用方法论
- 可复用模板
- 入库判断

不能把程序生成的 `selected_deep_cards/` 直接视为正式深学卡。它们只是候选素材和机器分析结果。

## 7. 全量粗扫检查结论

### 已完成

2026-06-17 的轻量扫描已经识别：

- 姜胡说去重内容：573条
- 已输出账号级方向数量、Top榜和热度数据
- `latest_direction_rankings.json` 中姜胡说方向榜共240行、102个唯一视频

### 已补齐（2026-06-19）

通用跨平台粗扫引擎已经生成并验收姜胡说全量粗扫产物：

- 覆盖573个唯一视频，账号全部为姜胡说。
- 每条内容只有一个主方向，并保留辅方向标签。
- 13个方向文件合计覆盖573条。
- 标记127条深学目标和446条非本轮深学内容。
- 已有12条深学内容全部保留在127条权威范围内。
- 两条无视频内容保留粗扫记录，但排除深学。
- 123条低置信异常全部审核，94条校正主方向，最终待审核为0。

已生成：

```text
01_Case_Cleaning/content_rough_scan/jianghushuo/
├── all_content_inventory.jsonl
├── all_content_inventory.md
├── deep_learning_scope.json
├── review_overrides.json
├── validation_report.json
└── directions/{13个方向}/粗扫内容和选题.md
```

每条粗扫记录至少包含：

- `source_id`
- 原视频链接
- 标题/主题摘要
- 唯一主方向
- 辅方向标签
- 热度指标
- 粗扫价值
- 是否进入127条深学范围
- 是否候补深学
- 暂不深学原因

验收结果：

- 账号全部为姜胡说。
- 573个唯一 `source_id` 全覆盖，不重复、不遗漏。
- 每条只有一个主方向。
- 13个方向文件合计覆盖573条。
- 深学127条、非深学446条。
- 验证报告：`valid: true`，错误数0。
- 产物仍为候选粗扫内容，未写正式知识库。

如果代码重新读取原始元数据以生成全量清单，只允许只读；不得修改 `数据/` 和 `00_Inbox/`。

## 7.1 代码与 Codex 职责边界

全量粗扫必须以代码批处理为主，禁止让 Codex 人工逐条处理573条视频。

### 必须由代码完成

- 读取并去重姜胡说573条元数据。
- 精确过滤账号，禁止混入其他账号。
- 为每条内容计算13方向相关度、热度和内容完整度。
- 为每条内容分配一个主方向和多个辅方向标签。
- 根据统一规则生成粗扫价值、候补深学价值和暂不深学原因。
- 标记127条已选深学内容和446条非本轮深学内容。
- 生成一份573条完整 JSONL/Markdown 清单。
- 自动拆分生成13个方向的 `粗扫内容和选题.md`。
- 自动检查总数、重复ID、遗漏ID、跨方向重复主归属和账号污染。
- 输出低置信条目的 `needs_review: true` 清单，交给 Codex审核。

### Codex 只负责

- 在代码运行前定义和审核分类规则、价值判断规则。
- 审核代码标记为 `needs_review: true` 的低置信、并列或未归类内容。
- 每个方向抽样复核，不逐条人工阅读573条。
- 基于代码产物提炼高频选题、共性结构和方向方法论。
- 对正式入库内容做最终判断。

### Codex 禁止行为

- 禁止手工逐条打开573条内容并自行建立清单。
- 禁止手工复制粘贴生成13个方向文件。
- 禁止绕过代码覆盖校验后声称全量粗扫完成。
- 禁止把自己的推断直接写成全部视频的事实字段。
- 禁止因为少量分类不确定就接管整批机械任务。

正确流程：

```text
代码生成573条基础粗扫结果
→ 代码输出低置信/冲突清单
→ Codex只审核异常项和抽样质量
→ 代码合并修正并重新校验
→ 代码生成13方向粗扫文件
→ Codex提炼方向规律和方法论
```

## 7.2 现有代码能力自检

检查时间：2026-06-19

结论：通用跨平台粗扫代码已经补齐并通过实际数据验收。姜胡说只是首个profile；后续账号通过新增配置复用同一引擎。

| 能力 | 当前状态 | 证据/缺口 |
|---|---|---|
| 读取、标准化、去重视频元数据 | 已具备 | `tools/video_learning.py` 的 `load_unique_records()` |
| 精确过滤姜胡说账号 | 已具备 | 扫描和下载规划均支持账号过滤 |
| 热度指标计算 | 已具备 | `heat_score()` |
| 多方向关键词识别 | 已具备 | `detect_directions()`，但属于多标签结果 |
| 全量内容清单基础结构 | 部分具备 | `content_inventory_rows()` 存在，但当前产物未生成 |
| 573条全量清单文件 | 已完成 | `content_rough_scan/jianghushuo/all_content_inventory.jsonl` |
| 稳定的单主方向分类 | 已完成 | profile核心词/辅助词权重、置信差和人工覆盖机制 |
| 13方向“技能沉淀”分类 | 已完成 | 已纳入姜胡说profile和全量产物 |
| 13方向全量粗扫文件 | 已完成 | `content_rough_scan/jianghushuo/directions/` |
| 粗扫价值/候补价值/暂不深学原因 | 已完成 | 全量inventory和方向文件均已生成 |
| 低置信异常清单 | 已完成 | 123条已审核，覆盖记录保存在 `review_overrides.json` |
| 573条覆盖和唯一性校验 | 已完成 | `validation_report.json` 为有效 |

`tools/jianghushuo_download_plan.py` 仍只负责下载目标规划；全量粗扫统一由通用引擎负责。

### 已实现的通用代码

建议新增独立模块：

`tools/content_rough_scan.py`

已实现职责：

1. 复用 `load_unique_records()`，过滤姜胡说。
2. 复用13方向定义和相关度评分，不只处理Top10。
3. 为573条内容生成唯一主方向、辅方向、置信分和冲突原因。
4. 用规则生成粗扫价值、候补深学和暂不深学原因。
5. 对低置信或并列第一的条目标记 `needs_review: true`。
6. 写出全量 inventory 和13个方向文件。
7. 校验573条全覆盖、唯一主归属和127/446范围一致。

必须先写测试，至少覆盖：

- 其他账号不会进入结果。
- 573条输出ID唯一。
- 一条视频只有一个主方向。
- 主方向并列时进入 `needs_review`，不静默按字典顺序决定。
- 127条深学目标正确标记，排除的两条不进入深学范围。
- 13个方向文件合计恰好覆盖573条。
- 重复ID、遗漏ID或账号污染会导致命令非零退出。

代码测试和实际验证均已通过。Codex只处理了123条异常审核清单，没有人工接管573条主体工作。

## 8. 后续正式深学任务

1. 读取本交接包和固定结构规范。
2. 使用 `deep_learning_scope.json` 作为127条唯一权威深学范围。
3. 将已有12条合集拆为独立知识卡，禁止重复学习。
4. 从赚钱方向剩余6条开始，按3+3连续深学。
5. 完成赚钱方向方法论总结后，提交用户审核，再进入创业方向。

## 9. 当前状态边界

- 下载阶段已结束。
- 127条目标素材完整。
- 2条无视频内容按用户决定排除，不再补下载。
- 真正深学完成12/127，剩余115。
- 全量粗扫知识已完成：573/573，13方向文件齐全，验证通过。
