# 账号 Skill 中文可见层升级审批记录

- 状态：已确认并生效
- 日期：2026-07-16
- 影响流程：account-learning 2.4 阶段 6 封装
- 用户确认：用户在指出四个正式账号文件不一致后明确回复“我觉得可以补齐一下”。

## 原问题

账号 Skill v2.2 重建保留了机器调用所需的 `SKILL.md` 与四个 references，但没有继续交付用户原来能直接查看的中文账号文件。四个正式账号中，只有“省钱也要喂饱自己（沪漂版）”保留 `账号整体方法论.md`；其余账号的防 AI 味与生产机制藏在 references，账号根目录没有可见入口。

## 已批准行为

每个账号候选包和正式账号中心统一交付：

1. `账号整体方法论.md`
2. `内容生产使用说明.md`
3. `减少AI味输出规则.md`
4. `内容输出标准模板.md`

四份文件只能聚合本账号已批准方法、正式 references 和边界；不得从方向词、平台词、其他账号模板或候选方法补造。`skill/` 保持唯一权威源，中文文件声明账号 Skill 版本并随版本同步。

## 已执行范围

- 升级 `account_skill_contract.json`、`account_learning_pipeline.json` 和 account-learning 阶段 6 封装说明。
- 为姜胡说、李宗恒、小森林的小世界、省钱也要喂饱自己（沪漂版）补齐四份中文可见文件。
- 修复四个账号索引中的可见入口与失效 `docs/legacy/` 提示。
- 增加缺文件、缺权威来源标记和版本不一致校验。
- 将四份可见文件同步回四个阶段 6 候选包。

## 回滚

移除四份中文视图、契约中的 `required_account_view_files` 和阶段 6 的 `required_account_views` 即可；账号正式 Skill 五件套与生产调用不受影响。

## 验证结果

- 四个正式账号均有四份中文可见文件，并包含权威来源标记与对应账号 Skill 版本。
- 四个阶段 6 候选包同步完成，逐工作流验证通过。
- `account-skills-sync`：4 个账号注册成功，索引无缺失路径。
- `validate-system`：通过。
- `python -m unittest discover -s tests`：197 项测试通过。
